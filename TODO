High
* take input list of tasks: sort, handle priority

Medium
* generate html web page from python
* extend future term page: needs more real data

Low
* take input list of tasks: handle parent
* dynamic web page: pan, zoom
* text wrap function - break on characters if a line is too long
* interactivity support: filtering, change priority on a task and see the update
* interactivity support: edit task title, edit task duration, create new task
